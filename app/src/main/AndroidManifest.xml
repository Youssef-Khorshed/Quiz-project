<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iu"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iu"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizProject">
        <activity android:name=".result"></activity>
        <activity android:name=".Quiz_site5" />
        <activity android:name=".Quiz_site4" />
        <activity android:name=".Quiz_site3" />
        <activity android:name=".Quiz_site2" />
        <activity android:name=".Quiz_site1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>